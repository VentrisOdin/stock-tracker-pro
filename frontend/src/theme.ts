
import { extendTheme } from "@chakra-ui/react";
const theme = extendTheme({
  fonts: { heading: "Inter, system-ui", body: "Inter, system-ui" },
  styles: { global: { body: { bg: "#0b0f1a", color: "#e5e7eb" } } }
});
export default theme;
